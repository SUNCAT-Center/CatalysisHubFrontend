// import React from 'react';
// import { shallow } from 'enzyme';

// import PublicationView from '../index';

describe('<PublicationView />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
