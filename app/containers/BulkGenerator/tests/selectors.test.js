// import { fromJS } from 'immutable';
// import { makeSelectBulkGeneratorDomain } from '../selectors';

// const selector = makeSelectBulkGeneratorDomain();

describe('makeSelectBulkGeneratorDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
