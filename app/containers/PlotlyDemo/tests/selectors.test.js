// import { fromJS } from 'immutable';
// import { makeSelectPlotlyDemoDomain } from '../selectors';

// const selector = makeSelectPlotlyDemoDomain();

describe('makeSelectPlotlyDemoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
