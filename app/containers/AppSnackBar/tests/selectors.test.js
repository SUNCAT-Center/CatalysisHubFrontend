// import { fromJS } from 'immutable';
// import { makeSelectAppSnackBarDomain } from '../selectors';

// const selector = makeSelectAppSnackBarDomain();

describe('makeSelectAppSnackBarDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
