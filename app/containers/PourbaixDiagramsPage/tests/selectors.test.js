// import { fromJS } from 'immutable';
// import { makeSelectPourbaixDiagramsPageDomain } from '../selectors';

// const selector = makeSelectPourbaixDiagramsPageDomain();

describe('makeSelectPourbaixDiagramsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
