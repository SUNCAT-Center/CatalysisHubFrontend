// import { fromJS } from 'immutable';
// import { makeSelectScalingRelationsPageDomain } from '../selectors';

// const selector = makeSelectScalingRelationsPageDomain();

describe('makeSelectScalingRelationsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
