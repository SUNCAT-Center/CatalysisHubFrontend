// import { fromJS } from 'immutable';
// import { makeSelectPeriodicTableSelectorDomain } from '../selectors';

// const selector = makeSelectPeriodicTableSelectorDomain();

describe('makeSelectPeriodicTableSelectorDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
