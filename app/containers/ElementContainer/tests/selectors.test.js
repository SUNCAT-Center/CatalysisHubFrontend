// import { fromJS } from 'immutable';
// import { makeSelectElementContainerDomain } from '../selectors';

// const selector = makeSelectElementContainerDomain();

describe('makeSelectElementContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
