// import { fromJS } from 'immutable';
// import { makeSelectUploadDomain } from '../selectors';

// const selector = makeSelectUploadDomain();

describe('makeSelectUploadDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
